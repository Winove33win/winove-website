# Backend environment variables
# Duplicate this file to backend/.env and adjust values for your environment.

# --- Application ---
APP_BASE_URL=https://www.example.com # Base URL used for canonical links and redirects
PUBLIC_BASE_URL=https://www.example.com # Base público para geração de sitemap e templates
PORT=3333 # Porta HTTP exposta pelo servidor Express
NODE_ENV=production # Modo do Node.js (development/production)

# --- Database (MySQL) ---
DB_HOST=localhost # Host ou IP do banco de dados
DB_PORT=3306 # Porta do MySQL
DB_USER=winove_user # Usuário com acesso às tabelas da aplicação
DB_PASSWORD=super_secret_password # Senha do usuário do banco
DB_NAME=winove_database # Nome do schema
DB_CONN_LIMIT=10 # Número máximo de conexões simultâneas no pool

# --- Painel comercial ---
COMMERCIAL_PANEL_USERNAME=comercial # Usuário básico para /comercial-propostas
COMMERCIAL_PANEL_PASSWORD=senha-muito-secreta # Senha obrigatória para liberar o painel

# --- E-mail/SMTP ---
MAIL_HOST=smtp.example.com # Servidor SMTP
MAIL_PORT=587 # Porta SMTP (465 para SSL explícito)
MAIL_USER=mailer@example.com # Usuário/conta SMTP
MAIL_PASS=mailer_password # Senha SMTP
MAIL_FROM=Winove <contato@example.com> # Remetente padrão dos envios
CONTACT_EMAIL=contato@example.com # Destino para formulários de contato

# --- Leads ---
LEADS_TABLE=leads # Nome da tabela de leads
LEADS_LIBRAS_TABLE=leads_libras # Nome da tabela de leads libras

# --- SEO e conteúdo ---
DEFAULT_POST_AUTHOR=Equipe Winove # Autor padrão dos posts
PUBLISHER_NAME=Winove # Nome da organização para metadata
PUBLISHER_LOGO=https://www.example.com/assets/logo.png # Logo usada em dados estruturados
DEFAULT_SHARE_IMAGE=https://www.example.com/assets/share.png # Imagem padrão de compartilhamento

# --- Geração de sitemap ---
SITEMAP_OUTPUT=/var/www/vhosts/example.com/httpdocs/sitemap.xml # Caminho absoluto do arquivo gerado

# --- Renderização SSR opcional ---
SSR_INDEX_FILE=dist/index.html # Caminho alternativo para o arquivo index.html do frontend (opcional)
