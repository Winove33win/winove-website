# Example environment configuration for winove_api_appuni.js
# Copy this file to `.env` before starting the standalone API on AppUni/Plesk.
# Replace the placeholder values with your real credentials.

DB_HOST=<database-host>
DB_PORT=<database-port>
DB_USER=<database-user>
DB_PASS=<database-password>
DB_NAME=<database-name>
LEADS_TABLE=leads
LEADS_LIBRAS_TABLE=leads_libras

MAIL_HOST=smtp.seuprovedor.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=usuario@dominio.com
MAIL_PASS=senha_do_email
MAIL_FROM=notificacoes@winove.com.br
MAIL_SUBJECT="Novo lead recebido"

CONTACT_EMAIL=destinatario@winove.com.br

PORT=3000
