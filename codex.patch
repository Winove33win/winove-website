*** Begin Patch
*** Update File: frontend/src/pages/TemplateDetail.tsx
@@ /* Price Card */
-                   <div className="space-y-3">
-                     <Button 
-                       onClick={handlePurchase}
-                       className="w-full btn-primary" 
-                       size="lg"
-                     >
-                       <Download className="w-5 h-5 mr-2" />
-                       Comprar Template
-                     </Button>
-                     
-                     <a 
-                       href={template.demoUrl} 
-                       target="_blank" 
-                       rel="noopener noreferrer"
-                       className="block"
-                     >
-                       <Button variant="outline" className="w-full" size="lg">
-                         <Eye className="w-5 h-5 mr-2" />
-                         Ver Demo
-                       </Button>
-                     </a>
-                   </div>
+                   <div className="space-y-3">
+                     {/* WhatsApp purchase buttons using optional plans */}
+                     {/* Helper to build WhatsApp link */}
+                     {(() => {
+                       const wa = template.contact?.whatsappIntl ?? '5519982403845';
+                       const baseMsg = template.contact?.defaultMessage ?? 'Olá! Vim da página do Template Advocacia Blue Mode.';
+                       const buildWaLink = (msg: string) => {
+                         const href = typeof window !== 'undefined' ? window.location.href : '';
+                         const sep = href.includes('?') ? '&' : '?';
+                         const url = href ? `${href}${sep}utm_source=template-advocacia` : 'https://winove.com.br/templates';
+                         return `https://wa.me/${wa}?text=${encodeURIComponent(`${baseMsg} ${msg} | Página: ${url}`)}`;
+                       };
+                       return (
+                         <>
+                           <a
+                             href={buildWaLink('Quero comprar o Template (R$ ' + template.price.toFixed(2).replace('.', ',') + ').')}
+                             target="_blank"
+                             rel="noopener noreferrer"
+                             className="block w-full"
+                           >
+                             <Button className="w-full btn-primary" size="lg">
+                               <Download className="w-5 h-5 mr-2" />
+                               {template.ctaTexts?.buyTemplate ?? `Comprar Template — R$ ${template.price.toFixed(2).replace('.', ',')}`}
+                             </Button>
+                           </a>
+                           {template.addons?.hosting && (
+                             <a
+                               href={buildWaLink('Quero adicionar Hospedagem Plesk 3GB (R$ ' + template.addons.hosting.priceYear.toFixed(2).replace('.', ',') + '/ano).')}
+                               target="_blank"
+                               rel="noopener noreferrer"
+                               className="block w-full"
+                             >
+                               <Button variant="outline" className="w-full" size="lg">
+                                 {`Adicionar Hospedagem Plesk 3GB — R$ ${template.addons.hosting.priceYear.toFixed(2).replace('.', ',')}/ano`}
+                               </Button>
+                             </a>
+                           )}
+                           {template.addons?.email && (
+                             <a
+                               href={buildWaLink('Quero adicionar E-mail Corporativo 3GB (R$ ' + template.addons.email.priceYear.toFixed(2).replace('.', ',') + '/ano).')}
+                               target="_blank"
+                               rel="noopener noreferrer"
+                               className="block w-full"
+                             >
+                               <Button variant="outline" className="w-full" size="lg">
+                                 {`Adicionar E-mail Corporativo 3GB — R$ ${template.addons.email.priceYear.toFixed(2).replace('.', ',')}/ano`}
+                               </Button>
+                             </a>
+                           )}
+                           {template.bundles?.[0] && (
+                             <a
+                               href={buildWaLink('Quero o Combo: Site + Hospedagem + E-mail (1º ano R$ ' + template.bundles[0].firstYear.toFixed(2).replace('.', ',') + '; renovação R$ ' + template.bundles[0].renewalYear.toFixed(2).replace('.', ',') + '/ano).')}
+                               target="_blank"
+                               rel="noopener noreferrer"
+                               className="block w-full"
+                             >
+                               <Button className="w-full btn-highlight" size="lg">
+                                 {template.ctaTexts?.bundle ?? `Combo (Site + Hospedagem + E-mail) — R$ ${template.bundles[0].firstYear.toFixed(2).replace('.', ',')} (1º ano)`}
+                               </Button>
+                             </a>
+                           )}
+                           <a 
+                             href={template.demoUrl} 
+                             target="_blank" 
+                             rel="noopener noreferrer"
+                             className="block"
+                           >
+                             <Button variant="outline" className="w-full" size="lg">
+                               <Eye className="w-5 h-5 mr-2" />
+                               Ver Demo
+                             </Button>
+                           </a>
+                         </>
+                       );
+                     })()}
+                   </div>
*** End Patch

*** Update File: frontend/src/lib/api.ts
@@ export interface Template {
   pages?: number;
+  contact?: {
+    whatsappIntl?: string;
+    defaultMessage?: string;
+  };
+  ctaTexts?: {
+    buyTemplate?: string;
+    hosting?: string;
+    email?: string;
+    bundle?: string;
+  };
+  addons?: {
+    hosting?: {
+      id?: string;
+      name?: string;
+      priceYear?: number;
+      storageGB?: number;
+      panel?: string;
+    };
+    email?: {
+      id?: string;
+      name?: string;
+      priceYear?: number;
+      quotaGB?: number;
+      accounts?: number;
+    };
+  };
+  bundles?: {
+    id: string;
+    name: string;
+    firstYear?: number;
+    renewalYear?: number;
+    includes?: string[];
+    cta?: string;
+  }[];
*** End Patch

*** Update File: backend/routes/templates.js
@@ const normalize = (row) => {
     updated_at: row.updated_at,
+    contact: meta.contact || {},
+    ctaTexts: meta.ctaTexts || {},
+    addons: meta.addons || {},
+    bundles: meta.bundles || [],
   };
*** End Patch
